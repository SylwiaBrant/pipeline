#!/usr/bin/env groovy

pipelineJob('build-jenkins-operator-service') {
    displayName('Build Jenkins Operator Service')

    definition {
        cpsScm {
            scm {
                git {
                    remote {
                        url('https://github.com/SylwiaBrant/pipeline.git')
                       // credentials('jenkins')
                    }
                    branches('*/master')
                }
            }
            scriptPath('cicd/pipelines/build.jenkins')
        }
    }
}
